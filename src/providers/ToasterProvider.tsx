"use client";

// * after add package react-hot-toast it's for Smoking hot Notifications for React.
import { Toaster } from "react-hot-toast";


const ToasterProvider = () => {
    return (
        <Toaster />
    )
}

export default ToasterProvider